npm install express express-validator bcrypt config gravatar jsonwebtoken mongoose request
npm install -D nodemon concurrently   
concurrently -> allows to run backend and front end with single command